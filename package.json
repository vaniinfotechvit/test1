{
  "name": "simple-browser-task-app",
  "version": "1.0.0",
  "private": true,
  "description": "A simple browser-only task app for GitHub Actions practice",
  "scripts": {
    "test": "node --test"
  }
}
